import React from 'react';
import logo from '../../logo.svg';

const Logo = () => {
    return (
        <a href="/">
            <img src={ logo } alt="Site logo"/>
        </a>
    );
};

export default Logo;
